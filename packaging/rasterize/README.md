# Logo rasterizer

Regenerates the PNG/ICO/ICNS icon set from `assets/logo-badge.svg` and
`assets/logo-mark.svg`. Run from the repo root:

```
cargo run --release --manifest-path packaging/rasterize/Cargo.toml -- .
```
